//? Librerias
import styled from 'styled-components/macro'

export const StyledSelect = styled.select`
  //? evitamos que los navegadores agregen estilos por defecto
  appearance: none;
  -webkit-appearance: none;
  -moz-appearance: none;

  padding: 7px 15px;
  width: 150px;
  font-size: 1rem;
  font-weight: 600;
  color: #b0b0b0;
  border-radius: 5px;
  background-color: transparent;
`
